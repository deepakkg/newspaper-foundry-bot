name: Tweet Bot

on:
  schedule:
    # GitHub cron uses UTC. These four entries map to:
    # 08:00, 12:00, 16:00, and 20:00 in Asia/Kolkata.
    - cron: "30 2 * * *"
    - cron: "30 6 * * *"
    - cron: "30 10 * * *"
    - cron: "30 14 * * *"
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: tweet-bot
  cancel-in-progress: false

jobs:
  tweet:
    runs-on: ubuntu-latest
    env:
      LLM_BASE_URL: ${{ vars.LLM_BASE_URL }}
      LLM_MODEL: ${{ vars.LLM_MODEL }}
      LLM_API_KEY: ${{ secrets.LLM_API_KEY }}
      TOPICS: ${{ vars.TOPICS }}
      TONES: ${{ vars.TONES }}
      MAX_TWEET_CHARS: ${{ vars.MAX_TWEET_CHARS || '230' }}
      MAX_RETRIES: ${{ vars.MAX_RETRIES || '5' }}
      LLM_TIMEOUT_SECONDS: ${{ vars.LLM_TIMEOUT_SECONDS || '120' }}
      NEWS_ENABLED: ${{ vars.NEWS_ENABLED || 'true' }}
      NEWS_RECENCY_HOURS: ${{ vars.NEWS_RECENCY_HOURS || '48' }}
      NEWS_REGION: ${{ vars.NEWS_REGION || 'US' }}
      NEWS_LANGUAGE: ${{ vars.NEWS_LANGUAGE || 'en' }}
      POST_TO_BLUESKY: ${{ vars.POST_TO_BLUESKY || 'false' }}
      BLUESKY_HANDLE: ${{ vars.BLUESKY_HANDLE }}
      BLUESKY_SERVICE_URL: ${{ vars.BLUESKY_SERVICE_URL || 'https://bsky.social' }}
      BLUESKY_APP_PASSWORD: ${{ secrets.BLUESKY_APP_PASSWORD }}
      POST_TO_INSTAGRAM: ${{ vars.POST_TO_INSTAGRAM || 'false' }}
      INSTAGRAM_ACCOUNT_ID: ${{ vars.INSTAGRAM_ACCOUNT_ID }}
      INSTAGRAM_GRAPH_BASE_URL: ${{ vars.INSTAGRAM_GRAPH_BASE_URL || 'https://graph.instagram.com' }}
      INSTAGRAM_GRAPH_API_VERSION: ${{ vars.INSTAGRAM_GRAPH_API_VERSION || 'v23.0' }}
      INSTAGRAM_ACCESS_TOKEN: ${{ secrets.INSTAGRAM_ACCESS_TOKEN }}
      CLOUDINARY_CLOUD_NAME: ${{ vars.CLOUDINARY_CLOUD_NAME }}
      CLOUDINARY_FOLDER: ${{ vars.CLOUDINARY_FOLDER || 'content-bot' }}
      CLOUDINARY_API_KEY: ${{ secrets.CLOUDINARY_API_KEY }}
      CLOUDINARY_API_SECRET: ${{ secrets.CLOUDINARY_API_SECRET }}
      POST_TO_X: ${{ vars.POST_TO_X || 'false' }}
      X_API_KEY: ${{ secrets.X_API_KEY }}
      X_API_KEY_SECRET: ${{ secrets.X_API_KEY_SECRET }}
      X_ACCESS_TOKEN: ${{ secrets.X_ACCESS_TOKEN }}
      X_ACCESS_TOKEN_SECRET: ${{ secrets.X_ACCESS_TOKEN_SECRET }}
      X_USERNAME: ${{ vars.X_USERNAME }}
      APPROVAL_REQUIRED: ${{ vars.APPROVAL_REQUIRED || 'true' }}
      APPROVAL_TIMEOUT_MINUTES: ${{ vars.APPROVAL_TIMEOUT_MINUTES || '90' }}
      TELEGRAM_NOTIFICATIONS_ENABLED: ${{ vars.TELEGRAM_NOTIFICATIONS_ENABLED || 'false' }}
      TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
      TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
      DISCORD_NOTIFICATIONS_ENABLED: ${{ vars.DISCORD_NOTIFICATIONS_ENABLED || 'false' }}
      DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}
      DISCORD_BOT_TOKEN: ${{ secrets.DISCORD_BOT_TOKEN }}
      DISCORD_CHANNEL_ID: ${{ vars.DISCORD_CHANNEL_ID }}
      DISCORD_APPROVER_USER_IDS: ${{ vars.DISCORD_APPROVER_USER_IDS }}
      GENERATED_IMAGE_DIR: generated-posts
      LOG_FILE_PATH: tweet-log-store/tweet-history.md
      TWEET_HISTORY_BRANCH: tweet-history

    steps:
      - name: Check out app code
        uses: actions/checkout@v6

      - name: Check out post history
        uses: actions/checkout@v6
        with:
          ref: tweet-history
          path: tweet-log-store

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"
          cache: pip

      - name: Install dependencies
        run: python -m pip install -r requirements.txt

      - name: Generate and publish post
        run: python tweet_generator.py

      - name: Commit post history
        run: |
          git -C tweet-log-store config user.name "github-actions[bot]"
          git -C tweet-log-store config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git -C tweet-log-store add tweet-history.md
          if git -C tweet-log-store diff --cached --quiet; then
            echo "No post history changes to commit."
          else
            git -C tweet-log-store commit -m "Update post history"
            git -C tweet-log-store push origin "HEAD:${TWEET_HISTORY_BRANCH}"
          fi
